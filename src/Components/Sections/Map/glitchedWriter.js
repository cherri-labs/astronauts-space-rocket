import GlitchedWriter, {
//  wait
} from 'glitched-writer';

export default async function glitchedWriter() {
  /*
     const Writer = new GlitchedWriter('#glitch__this', { letterize: true });

     await wait(1000);
     await Writer.write("Glitch this.");
   */
}
